name: probe-q1-from-main
on:
  workflow_dispatch: {}

jobs:
  probe:
    name: probe-job-main
    runs-on: ubuntu-latest
    steps:
      - name: emit marker
        run: echo "PROBE-WORKFLOW-FROM=main"
